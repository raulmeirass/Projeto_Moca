<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor de fundo transparente -->
    <solid android:color="@android:color/transparent" />

    <!-- Borda colorida -->
    <stroke
        android:width="1dp"
        android:color="@color/cinza" /> <!-- Cor da borda (vermelha no exemplo) -->

    <!-- Raio dos cantos (opcional) -->
    <corners android:radius="10dp" /> <!-- Defina o raio desejado -->
</shape>